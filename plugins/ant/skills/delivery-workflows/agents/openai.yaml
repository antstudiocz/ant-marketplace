interface:
  display_name: "(ant) Delivery Workflows"
  short_description: "Context-aware merge-conflict resolution"
  default_prompt: "Use $delivery-workflows only to resolve merge conflicts. Use $merge-request separately for PR/MR creation or updates."
