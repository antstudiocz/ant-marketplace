# Startup Contract Template

Use after repository/runtime discovery. Ask only unresolved user-owned decisions.

```text
Planning cadence:
Phase approval policy:
Execution / decision mode:
Commit strategy:
Delivery boundary:
MR intent and language:
Pipeline check/recovery policy:
Browser validation policy:
Stop conditions:
Confirmed target branch:
Unrelated-change decision:
```

Metadata is a display summary only. Persist mutation authorization separately under `../policies/approval-policy.md`.
