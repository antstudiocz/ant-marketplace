interface:
  display_name: "(ant) Implementation Orchestrator"
  short_description: "Coordinate proportional implementation, review, validation, and delivery"
  default_prompt: "Use $implementation-orchestrator to inspect the repository, choose a proportional agent shape, adapt reasoning during work, validate the final result, and perform only the requested delivery actions."
