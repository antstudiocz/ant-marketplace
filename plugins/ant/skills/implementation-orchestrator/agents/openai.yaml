interface:
  display_name: "(ant) Implementation Orchestrator"
  short_description: "Plan, phase, implement, verify, and deliver with explicit run controls"
  default_prompt: "Use $implementation-orchestrator to drive this task from git setup through planning, phased implementation, verification, commits, and delivery."
