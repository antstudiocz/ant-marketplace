interface:
  display_name: "(ant) Implementation Orchestrator"
  short_description: "Plan, approve, implement, verify, and deliver"
  default_prompt: "Use $implementation-orchestrator to discover the repository, brainstorm new behavior with me, obtain approval for a concrete plan before edits, then implement and verify it proportionally."
