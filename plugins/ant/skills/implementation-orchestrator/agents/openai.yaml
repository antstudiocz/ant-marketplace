interface:
  display_name: "(ant) Implementation Orchestrator"
  short_description: "Plan, delegate, review, and verify implementation"
  default_prompt: "Use $implementation-orchestrator to discover, plan, delegate, review, and verify this work proportionally."
