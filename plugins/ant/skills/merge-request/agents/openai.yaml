interface:
  display_name: "(ant) Merge Request"
  short_description: "Prepare delivery or resolve merge conflicts"
  default_prompt: "Use $merge-request for this PR, MR, pipeline, or merge-conflict task."
