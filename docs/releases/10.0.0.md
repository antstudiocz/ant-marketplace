# 10.0.0 Breaking Change Summary

This change prepares the breaking 10.0 update: a smaller instruction-only orchestration workflow shared by Claude Code and Codex. It does not claim that a tag was created or a release was published.

## Changes

- Reduces `implementation-orchestrator` to one public skill file and one lifecycle reference.
- Routes agents by Strong, Balanced, and Fast capabilities instead of hardcoded model identifiers.
- Reassesses reasoning during implementation, escalating for new complexity and lowering it for deterministic segments.
- Runs targeted checks after coherent phases and one full suite on the final tree before delivery.
- Keeps unaffected work moving when the user sends status questions, corrections, or additive requests during implementation.
- Removes the custom state/event contract, generated runtime direction, synthetic eval harness, obsolete role/template matrix, and duplicate PR/MR bridge.
- Makes `merge-request` the sole PR/MR owner and keeps `delivery-workflows` focused on merge conflicts.
- Updates maintainer and user documentation for the same behavior in Claude Code and Codex.

Version 10 does not read or migrate earlier custom orchestrator run files. Start a new orchestrated task after installing the update.
