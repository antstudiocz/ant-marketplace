# 12.0.1

Version 12.0.1 is a compatible instruction-only patch release.

## Changes

- Makes the Codex root model and effort developer-selected; `gpt-5.6-sol` at High remains recommended guidance only, without a root-route dispatch hard-stop.
- Keeps explicit child and nested-child model, effort, availability, fresh-context, and `fork_turns="none"` enforcement fail-closed.
- Leaves Claude root gates unchanged and aligns the maintainer, installation, orchestrator, and plugin/catalog version metadata.

No runtime migration, orchestration database, or global helper installation is required.
