# Native Goal default with durable-plan fallback

- Status: ready; phase: delivery.
- Objective: make native Goals default-on for implementation without making their absence an authorization blocker, and update the existing draft PR 74.
- Authority: user-approved recommendation relayed from side thread 01a07b59-f46d-7740-a600-28c96c88dd0c explicitly requests this policy replacement. It overrides the former mandatory native Goal gate. Existing scoped PR delivery authority continues; no merge/release/deployment.
- Scope: canonical lifecycle, Codex adapter, skill router, AGENTS and relevant user docs. Preserve required root-owned plan, routes, delegated owner, independent reviewer, candidate validation and host approval boundaries. No custom runtime or automation.
- Continuity: in-place evolution in the existing isolated marketplace worktree. The native Goal slot contains an unrelated blocked Marketing-alignment objective; preserve it exactly. Its terminal blocked state has no active continuation for this work. This task uses its own durable plan and has no native Goal or guaranteed automatic cross-turn continuation.
- Decisions: use matching active Goal or create when tools and established host/user authority permit; missing tool/permission/verification falls back transparently to durable plan for already-authorized work. Do not ask a blocking question solely for optional Goal creation. Explicit requests requiring native Goal remain unsatisfied if unavailable. Unrelated collisions require separate assessment and never repurpose or mutate the other objective. Analysis-only remains read-only/no Goal. Resume reconciles actual Goal availability and scope with plan/git/task state.
- Ownership: fresh Luna High integration owner writes scoped instructions/docs only; independent Sol High reviewer is read-only; root alone writes this plan directory. No nested delegation.
- Checks: affected skill metadata, local links, whitespace, stale mandatory-gate search excluding historical plans/releases; independent semantic cases available/missing authority/missing tool/matching Goal/unrelated collision/explicit Goal requirement/analysis-only/recovery.
- Risks: conflating durable memory with native continuation, treating plugin preference as user consent, automatic continuation pursuing an unrelated Goal, hiding missing capabilities, retaining contradictory hard gates.
- Candidate: root records final review and checkpoint before freeze; later tracked edits require affected re-review.
- Resume from here: owner replaces the obsolete hard gate with canonical default/fallback policy and aligned summaries, then independent review.
- Implementation checkpoint: owner updated canonical lifecycle, Codex adapter and four active summaries; both Claude validations, JSON, links and whitespace pass. Root requests independent review of remaining router preflight wording and unrelated-active-Goal collision handling.
- Final review: independent Sol High approved after separating verified empty/completed, matching, unverified and unrelated/blocked states; classify before creation. The router no longer implies a mandatory Goal pass. Shared policy is canonical and adapters own default-on/optional preference and tool mechanics.
- Final validation: both Claude manifests, four JSON manifests, all three skill validators, changed Markdown links and whitespace passed. Semantic review covered explicit requirements, permission/tool failure, recovery and completed-goal reuse. No runtime/eval framework added.
- Retrospective: optional coordination must not become an authorization gate, but missing capability cannot be confused with a foreign objective or a guarantee of continuation. Freeze and deliver to PR 74; preserve the unrelated blocked Marketing Goal without terminal mutation.
